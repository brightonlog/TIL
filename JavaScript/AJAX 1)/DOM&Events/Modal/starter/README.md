# 'keydown' - addEventListener 
- 아무 키나 누르면 이벤트가 진행된다.
- 그런데 어떤 키가 눌려졌는지 어떻게 알까?
  - 예를 들어, ESC만 눌렀을 때 팝업을 닫으려고 한다.

## 'keydown'의 이벤트 저장
- keydown 이벤트가 나타나면 자바스크립트가 객체를 생성한다
- 그 객체에는 실제로 어떤 키보드 값이 눌려졌는지 등 이벤트 자체에 대한 모든 정보가 담겨있다.
- 핸들러 함수의 매개 변수를 지정하여 해당 객체에 접근할 수 있다.
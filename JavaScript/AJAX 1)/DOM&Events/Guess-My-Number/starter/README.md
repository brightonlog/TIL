# DOM
## DOM이란?
HTML 문서를 구조적으로 나타내는 것.
- DOM은 HTML과 js 코드 사이의 연결점임!
- DOM은 JS를 이용해 HTML의 요소와 스타일 접근하여 조작할 수 있게 해줌
  - 텍스트를 바꾼다든가, JS에서 CSS를 바꾼다든가 등등...
- HTML 페이지가 로드되면, 브라우저가 자동으로 DOM을 생성함
  - 나무처럼 생긴 애한테 저장함
  - 이 나무에선 HTML 요소 하나 하나가 객체임
- ![alt text](image.png)
- JS를 통해 각 노드(객체)에 접근하고, 조작할 수 있음